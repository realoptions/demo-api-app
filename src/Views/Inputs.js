import React from 'react'
import Async from "react-async"

export const Inputs=({})=>{
    <Async promiseFn={loadJson}>
        <Async.Resolved>
            {data=>(
                
            )}
        </Async.Resolved>
    </Async>
}